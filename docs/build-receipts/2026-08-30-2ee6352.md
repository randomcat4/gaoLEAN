# Server build receipt: `2ee6352`

- UTC completion check: `2026-08-30T07:05:19Z`
- Commit: `2ee635230cf45788bea9907e4e67fe45c355f986`
- Source layout: fresh single-branch clone in a separate server directory
- Toolchain: Lean `4.32.0`, Lake `5.0.0-src+8c9756b`, Mathlib `v4.32.0`
- Command: `lake build GaoLean GaoFormal.AxiomAudit`
- Result: `Build completed successfully (8853 jobs)`; exit code `0`
- Raw successful build-log SHA-256:
  `9db3db009a99adfe250fe0e6f3c799b928faecf9fd8178b98cf38383458b762a`

The fast repository-surface check passed with 201 Lean sources, 26 Markdown
tables, and 16 paper-manifest entries.  The checkout remained clean at the
exact commit before and after the successful full command.

The first cold attempt exhausted the server disk, and a later retry exposed
two project artifacts damaged by that interruption.  No mathematical result
was inferred from either failed attempt.  After deleting only disposable
task-created clones, the affected artifacts were rebuilt from source and the
full command above completed in the same clean exact checkout.

The three new endpoints report only the standard trusted axioms:

```text
'GaoLean.exists_nonempty_weightedZeroSum_finset_subset'
depends on axioms: [propext, Classical.choice, Quot.sound]
'GaoLean.exists_weightedSelection_of_mem_weightedStep1AffineSumset'
depends on axioms: [propext, Classical.choice, Quot.sound]
'GaoLean.GeneralWeightedStrongRecursionState.exists_liftedFullCore'
depends on axioms: [propext, Classical.choice, Quot.sound]
```

This receipt certifies honest extraction of variable-cardinality weighted
Step 1 witnesses, labelled weighted Davenport blocks, and occurrence-faithful
lifting of recursive full cores.  It does not certify fixed-cardinality
centered padding, parent full-core expansion, or the complete
equation-(3)--(9) state.  The arbitrary-weight GMO theorem therefore remains
`PARTIALLY_VERIFIED`.
