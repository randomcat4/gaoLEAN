# Milestone M52: Proposition 3.1 augmentation nilpotence closure

Status: `LEAN_CHECKED_FROM_INVARIANT_DATA`.

`GaoLean/PGPlusMinusGroupAlgebra.lean` now proves the sharp ideal-theoretic
pigeonhole statement: if a finite ideal is generated by `y_i` and
`y_i ^ r_i = 0`, then its power at `1 + sum_i (r_i - 1)` is zero. It then
specializes this to `y_i = [f_i]-[0]` in `AddMonoidAlgebra (ZMod p) A`.

For a finite additive generating list with `p ^ mu_i • f_i = 0`, Lean derives
augmentation-product nilpotence at `1 + sum_i (p ^ mu_i - 1)`, proves this
degree odd when `p` is odd, and concludes the manuscript's plus-minus half
bound. The source-facing theorem has exactly one external mathematical
identification left: the Olson formula saying this degree is `D(A)` for the
invariant-factor presentation of a finite abelian `p`-group.

Server verification used Lean 4.32.0 / Mathlib v4.32.0. The full build
completed 8738 jobs. The new declaration audits contain only `propext`,
`Classical.choice`, and `Quot.sound`; the forbidden-declaration scan and
`git diff --check` pass.
